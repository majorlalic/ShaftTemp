- 规则需要crud接口维护
- 不处理mq中的火警
- 告警状态为：枚举类 待确认，已确认，持续观察，误报
- 程序会读取raw_data吗，不读的话是不是字段越精简越好
- raw_data过大怎么办，按分区存储，一共78个设备，如果每个设备50个分区，可能每次数据推送都有3900条
- mq频率为74台表，假设每台表50个分区，每分钟一条数据，一天就是5328000条数据
- 告警是由mq驱动的吗，这样处理会不会频率很高

告警状态
状态包含
- 待确认
- 已确认
- 持续观察
- 误报

报警分为监测对象报警和终端报警, 需要支持相关配置

8. 报警合并规则
- 定温告警, 差温报警, 升温速率: 同一个监测对象, 未确认的告警都合并, 计入合并次数, 可查看被合并的所有告警
- 离线告警: 终端告警不做合并, 且需要记录首次报警, 二次报警, 三次报警
- 断纤告警: 断纤和离线告警类似

我需要你着重考虑下性能问题：
目前已知有74台表，假设每台表50个分区，每分钟一条数据，那么一天就是5328000条数据
