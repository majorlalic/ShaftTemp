## AlarmRuleEngine， AlarmRuleResolverService, DeviceResolverService, OfflineInspectionTask, RealtimeStateService中还存在使用appProperties读取配置，不应该是数据库读取配置吗？

## ODS_DWEQ_DM_DEVICE_D 表已存在，我已经把表名改成了ODS_DWEQ_DM_SHAFT_DEVICE_D 给我最小改动， 是不是只改@Table(name = "ODS_DWEQ_DM_DEVICE_D")注解和xml里的表名就行