<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csg.dgri.szsiom.sysmanage.model.ShaftFloorVO">

    <select id="findActiveById" parameterType="map" resultType="com.csg.dgri.szsiom.sysmanage.model.ShaftFloorVO">
        <![CDATA[
        select * from ODS_DWEQ_DM_SHAFT_FLOOR_D where id = #{id} and (deleted is null or deleted = 0)
        ]]>
    </select>

    <select id="findAllActiveByMonitorId" parameterType="map" resultType="com.csg.dgri.szsiom.sysmanage.model.ShaftFloorVO">
        <![CDATA[
        select * from ODS_DWEQ_DM_SHAFT_FLOOR_D where monitor_id = #{monitorId} and (deleted is null or deleted = 0) order by sort asc, id asc
        ]]>
    </select>

    <select id="findAllActiveByDeviceId" parameterType="map" resultType="com.csg.dgri.szsiom.sysmanage.model.ShaftFloorVO">
        <![CDATA[
        select * from ODS_DWEQ_DM_SHAFT_FLOOR_D where device_id = #{deviceId} and (deleted is null or deleted = 0) order by sort asc, id asc
        ]]>
    </select>

    <select id="findAllActiveByAreaTreeId" parameterType="map" resultType="com.csg.dgri.szsiom.sysmanage.model.ShaftFloorVO">
        <![CDATA[
        select f.*
        from ODS_DWEQ_DM_SHAFT_FLOOR_D f
        join ODS_DWEQ_DM_AREA_D a on a.id = f.area_id
        where (f.deleted is null or f.deleted = 0)
          and (a.deleted is null or a.deleted = 0)
          and (a.id = #{areaTreeId} or instr('/' || nvl(a.path_ids, '') || '/', '/' || #{areaTreeId} || '/') > 0)
        order by f.sort asc, f.id asc
        ]]>
    </select>

</mapper>
